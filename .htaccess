<IfModule mod_rewrite.c>
    RewriteEngine On

    # Ensure the /website/ folder is hidden in the URL
    RewriteCond %{REQUEST_URI} ^/laravel/triptracker/?$
    RewriteRule ^(.*)$ /laravel/triptracker/website/$1 [L,P]

    # Handle Authorization Header (if required for authentication)
    RewriteCond %{HTTP:Authorization} .
    RewriteRule .* - [E=HTTP_AUTHORIZATION:%{HTTP:Authorization}]

    # Redirect Trailing Slashes If Not A Folder...
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteCond %{REQUEST_URI} (.+)/$
    RewriteRule ^ %1 [L,R=301]

    # Send Requests to Front Controller (WordPress or Laravel)
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteRule ^ index.php [L]
</IfModule>
